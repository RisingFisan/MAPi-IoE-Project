# mapi-library 

## Arduino

Library to read sensor data from multiple sensors and send it to the gateway NODERED via MQTT.

Includes examples.

To install the library use the MAKEFILE in the root folder to generate the library file.

## Docker Compose
Install all necessary dependencies on raspberry pi:
- Node-RED
- Mosquitto MQTT Broker

